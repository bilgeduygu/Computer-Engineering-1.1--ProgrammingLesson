#include <stdio.h>

int main()
{
	int i, max, min;
	int a[3]={10,20,30};
	
	max=a[i];
	min=a[i];
	
	if(min<max)
	{
		return min=a[i];
	}
	else
	{
		return min+=(min+1,max);
	}
	printf("Maximum element in array = %d\n Min element in array=%d",max,min);

}
